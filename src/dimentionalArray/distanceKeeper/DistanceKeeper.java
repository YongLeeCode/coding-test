package dimentionalArray.distanceKeeper;

public class DistanceKeeper {
    // 안 되는 상황
    // p x축 연속 불가능
    // p y축 연속 불가능
    // pop x축 불가능
    // pop y축 불가능
    // p 대각선(왼위, 왼아래, 오른위, 오른아래) 불가능
    // 만약(px
    //     xp)같은 상황이면 가능


}
