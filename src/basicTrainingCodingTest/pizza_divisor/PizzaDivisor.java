package basicTrainingCodingTest.pizza_divisor;

public class PizzaDivisor {
    // 공약수(common divisor)
    // 두 수 이상의 여러 수의 공통된 약수

    // 최대 공약수 (GCD, Greatest Common Divisor)
    // 두 수 a, b의 최대공약수는 gcd(a, b) 또는 (a, b)로 나타냄
    // 예) gcd(72, 90) 2 * 3^2 = 18
    // 만약 gcd(a, b) = 1 이면, 두 수 a, b는 서로소(coprime)관계에 있다고 한다.

    // 최소 공배수 (LCM, Lowest Common Multiple)
    // 두 수 이상의 여러 수의 공통된 배수

}
